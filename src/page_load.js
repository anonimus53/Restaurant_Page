const functionOne = () => console.log('FUNCTION ONE!');

export { functionOne };
// leave only the <html>, <body>,
// and <div id="content"> tags and instead create any new  element 
// by using JavaScript only,
// e.g. by appending each new element to div#content
// once the page is first loaded